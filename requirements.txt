# Milestone 0 dependency set.
#
# datasets is pinned below 4.0 deliberately: 4.x moved audio decoding to
# torchcodec, which changes the shape of sample["audio"] and adds a native
# dependency. Not worth debugging during a smoke test. Revisit at Milestone 3.
datasets>=2.19,<4
transformers>=4.40,<5
torch>=2.2
soundfile>=0.12        # libsndfile bindings — datasets uses this to decode audio
librosa>=0.10          # resampling; needed from Milestone 2 for the narrowband condition
jiwer>=3.0             # WER/CER — unused in Milestone 0, installed now to save a step
huggingface_hub>=0.23
